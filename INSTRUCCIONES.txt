Copia estos archivos en la raíz de tu repositorio:

1) app.py
   Sustituye tu app.py actual.

2) assets/worldcup_2026.jpg
   Copia esta imagen en la carpeta assets.

Después:

git add .
git commit -m "Corregir cabecera final"
git push

Nota:
Esta versión evita que Streamlit muestre el HTML como texto porque el bloque HTML no contiene líneas indentadas que Markdown pueda interpretar como código.
